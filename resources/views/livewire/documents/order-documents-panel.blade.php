<div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div class="mb-4 flex items-center justify-between gap-3"><h3 class="text-lg font-semibold tracking-tight text-slate-900">Документи</h3><div class="text-xs text-slate-500">{{ $documents->count() }} документ(ів)</div></div><div class="mb-5 flex flex-wrap gap-2"><button type="button" wire:click="generateInvoice" class="rounded-xl bg-slate-900 px-3 py-2 text-sm font-medium text-white hover:bg-slate-800">Сформувати рахунок</button><button type="button" wire:click="generateIntakeAct" class="rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">Акт приймання</button><button type="button" wire:click="generateCompletionAct" class="rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">Акт виконаних робіт</button></div>@if ($documents->isEmpty())<x-empty-state title="Документів ще немає." description="Сформуйте перший документ із цього блоку." />@else<div class="space-y-3">@foreach ($documents as $document)<div class="rounded-2xl border border-slate-200 px-4 py-3"><div class="flex items-start justify-between gap-3"><div><div class="text-sm font-medium text-slate-900">{{ $document->type?->label() ?? $document->type }}</div><div class="mt-1 text-xs text-slate-500">{{ $document->document_number ?: 'Без номера' }} · {{ optional($document->document_date)->format('d.m.Y') }}</div></div><div class="text-right"><div class="text-xs text-slate-500">{{ $document->document_status?->value ?? $document->document_status }}</div>@if ($document->disk && $document->path)<a href="{{ route('app.documents.download', $document->id) }}" class="mt-2 inline-flex rounded-xl border border-slate-300 bg-white px-3 py-2 text-xs font-medium text-slate-700 hover:bg-slate-50">Завантажити</a>@endif</div></div></div>@endforeach</div>@endif</div>